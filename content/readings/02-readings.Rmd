---
title: "The Standard Models"
linktitle: "Week 2: The Standard Models"
toc: true
output:
  rmarkdown::html_document:
    toc: true
menu:
  readings:
    parent: Readings
    weight: 2
type: docs
weight: 1
---

`r blogdown::shortcode("aes-header")`

```{r libraries, message=FALSE, warning=FALSE, include=FALSE}
source(here::here("content", "com_libs.R"))
```

### Download Literature

<center>
```{r echo=FALSE,eval=TRUE,message=FALSE}
loc <- here::here("static", "readings", "EDP618Week2Readings.zip")

downloadthis::download_file(
  path = loc,
  output_name = "Week 2 Readings",
  button_label = "&nbsp;&nbsp;&nbsp;<span style='color:#ffffff'>Download</span>",
  button_type = "primary",
  has_icon = TRUE,
  icon = "fa fa-save",
  class = "hvr-sweep-to-left"
  )
```
</center>

### Read Some Text

```{r echo=FALSE}

paper <- tibble(
  
  title = c("<i>Integrating quantitative and qualitative
research: how is it done?</i>",
            "<i>Toward a Conceptual Framework for Mixed-Method Evaluation Designs</i>",
            "<i>Crossing anxious borders: teaching across the quantitative–qualitative ‘divide’</i>",
            "<i>Paradigms Lost and Pragmatism Regained: Methodological Implications of Combining Qualitative and Quantitative Methods</i>"
  ),
  
  Source = c("Bryman (2006)",
             "Greene, Caracelli & Graham (1989)",
             "Luttrell (2005)",
             "Morgan (2007)"),
  
  Arrow = c("internal",
            "internal",
            "internal",
            "internal"),
  
  Icon = c("", 
           "",
           "",
           ""),
  
  Site = c(
    "",
    "",
    "",
    ""
  ),
  
  .name_repair = "minimal"
  
)

```

<center>
```{r, echo=FALSE}
paper %>%
  mutate(Icon = case_when(
    str_detect(`Icon`, "slack") ~ '<img src="/logos/slack-ico.png" alt="Slack icon" width="25px">',
    str_detect(`Icon`, "ecampus") ~ '<img src="/logos/wvu-gold-ico.png" alt="Slack icon" width="25px">',
    str_detect(`Icon`, "library") ~ '<img src="/logos/library-ico.png" alt="Slack icon" width="25px">',
    TRUE ~ NA_character_
    )
  ) %>%
  mutate(Arrow = case_when(
    str_detect(`Arrow`, "external") ~ '<div style="font-size: 0.5rem;"><i class="fa fa-external-link fa-2x" aria-hidden="true" style="color:#559977"></i></div>',
    TRUE ~ NA_character_
    )
  ) %>%
  mutate(Site = na_if(Site, "")) %>%
  mutate(Link = case_when(
    !is.na(Icon) ~ paste0("<a href=",Site," target='_blank'>",Icon,"</a>"),
    TRUE ~ "Please check this week's download"
    )
  ) %>%
  select(-Icon, -Site, -Arrow) %>% 
  mutate_all(replace_na,"") %>%
  kbl(col.names = c("Title", "Citation", "Link"), 
      "html", 
      escape = FALSE,
      align = 'lllc') %>%
  kable_paper(full_width = FALSE) %>%
  column_spec(1, width = "30em") %>%
  column_spec(2, width = "20em") %>%
  column_spec(3, width = "10em") %>% 
  row_spec(0:nrow(paper), extra_css = 'vertical-align: middle !important;', color = "#f7f7f7", background = "transparent")
```
</center>
