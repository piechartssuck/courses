---
title: "Scratching the Surface of Quasi-Experimental Designs"
linktitle: "Week 7: Scratching the Surface of Quasi-Experimental Designs"
menu:
  readings:
    parent: Readings
    weight: 7
toc: true
output:
  rmarkdown::html_document:
    toc: true
type: docs
weight: 2
---

`r blogdown::shortcode("materials-header")`
  
### Read The Text

```{r echo=FALSE, message=FALSE}
library(tidyverse)
library(fontawesome)
library(knitr)
library(kableExtra)
```

```{r eval = TRUE, echo = FALSE, message=FALSE}
read_schedule <- read_csv("readings.csv",
                          show_col_types = FALSE) %>% 
                  as_tibble() %>%
                  mutate(Required = case_when(
                  Required == "checked" ~ as.character(fontawesome::fa("check-circle", fill = "#428bca", height = "15px"))
              )) %>%
                  mutate(across(everything(), as.character)) %>%
                    filter(Week == 7) %>% #CHANGE HERE
                    select(-Week) %>%
                  filter_all(any_vars(!is.na(.))) %>% 
                  mutate(
                    across(everything(), ~replace_na(.x, ""))
                  ) %>%
                select_if(~!all(is.na(.))) 
  
```

```{r message=FALSE, warning=FALSE, eval = TRUE, echo = FALSE}
kable(read_schedule, 
      "html", 
      escape = FALSE,
      align = 'lllccccccccc') %>%
  kable_paper(full_width = F) %>%
  column_spec(1, width = "15em") %>%
  column_spec(2, width = "20em") %>%
  column_spec(3, width = "7em") %>%
  column_spec(4, width = "10em") %>%
  column_spec(5, width = "10em") %>%
  column_spec(6, width = "5em") %>%
  column_spec(1:6, color = "#ffffff") %>%
  row_spec(0:2, extra_css = 'vertical-align: middle !important;', color = "#ffffff", background = "transparent")
```

### Read A Paper

```{r echo=FALSE}
unchecked <- as.character(fontawesome::fa("square", fill = "#63d6c6"))

checked <- as.character(fontawesome::fa("check-square", fill = "#63d6c6"))

pdf_link1 <- function() {
 paste0("<a href='/handouts/Marinescu,%20Lawlor,%20&%20Kording%20(2018).pdf' target='_blank'>", "<img src='/logos/pdf-ico.png' alt='Slack icon' width='40'>", "</a>")
}

readings <- tibble(
  
  paper <- c("Marinescu, Lawlor, & Kording (2018). Quasi-experimental causality in neuroscience and behavioural research. <i>nature: human beings</i> (2). 891–898. <a href='https://doi.org/10.1038/s41562-018-0466-5' target='_blank'>https://doi.org/10.1038/s41562-018-0466-5 </a>"
  ),
  
  download <- c(
    pdf_link1()
  ),
  
  .name_repair = "minimal"
  
)

```

<center>
```{r, echo=FALSE}
readings %>%
  kbl(col.names = c("Source", "Link"), 
      "html", 
      escape = FALSE,
      align = 'lc') %>%
  kable_paper(full_width = F) %>%
  column_spec(1, width = "30em", color = "#ffffff", extra_css = 'vertical-align: middle !important;') %>%
  column_spec(2, width = "10em", color = "#ffffff", extra_css = 'vertical-align: middle !important;') %>% 
  row_spec(0:1, extra_css = 'vertical-align: middle !important;', color = "#ffffff", background = "transparent") 

```
</center>
  
